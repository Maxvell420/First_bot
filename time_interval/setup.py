from setuptools import setup, find_packages

setup(
    name="time_interval",
    version="0.1.0",
    author="Hui s Goty",
    description="Даты и интервал времени",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
