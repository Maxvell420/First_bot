services:
    python:
        container_name: python
        build:
            context: ../
            dockerfile: tests/.docker/python/DockerFile
        volumes:
        - ../:/app
        ports:
          - "8000:8000"